{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: core APIs, the native file dialog, and the zoom the window's hotkeys drive (#1598 — on macOS and Linux Tauri implements Ctrl/Cmd + wheel through this command, so without it zoomHotkeysEnabled is a no-op).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom",
    "dialog:default"
  ]
}
