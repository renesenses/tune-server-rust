{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: core APIs plus the native file dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
